// Supabase connection — fill these in from your project's Settings → API.
// Both are SAFE to commit publicly: the anon key only works within the rules
// (row-level security) we set on the database.
window.SUPABASE_URL = "YOUR-PROJECT-URL";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
